/*import {CredixPasswordFieldTooltipDirective} from './template-password-field-tooltip.directive';

describe('CredixPasswordFieldTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixPasswordFieldTooltipDirective();
    expect(directive).toBeTruthy();
  });
});*/
