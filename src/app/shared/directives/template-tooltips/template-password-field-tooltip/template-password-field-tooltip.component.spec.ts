/*import {async, ComponentFixture, TestBed} from '@angular/core/testing';

import {TemplateContentTooltipComponent} from './template-password-field-tooltip.component';

describe('TemplateContentTooltipComponent', () => {
  let component: TemplateContentTooltipComponent;
  let fixture: ComponentFixture<TemplateContentTooltipComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [TemplateContentTooltipComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TemplateContentTooltipComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
